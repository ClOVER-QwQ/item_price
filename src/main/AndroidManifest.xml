<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <application
            android:allowBackup="true"
            android:dataExtractionRules="@xml/data_extraction_rules"
            android:fullBackupContent="@xml/backup_rules"
            android:icon="@mipmap/itemprice"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/itempriceround"
            android:supportsRtl="true"
            android:theme="@style/Theme.Item_price"
            tools:targetApi="31">
        <activity
                android:name=".MainActivity"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <meta-data
                    android:name="android.app.lib_name"
                    android:value=""/>
        </activity>
        <activity android:name=".AddFrame"
                ></activity>
        <activity android:name=".AboutFrame"
                ></activity>
        <activity android:name=".CalculateFrame"
                ></activity>
        <activity android:name=".ModifyFrame"
                ></activity>
        <activity android:name=".DoingFrame"
                ></activity>
    </application>

</manifest>